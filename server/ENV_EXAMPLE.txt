# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/subsmanager?retryWrites=true&w=majority

# JWT Secret (Generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Email Configuration (Gmail SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=SubsManager AI <your-email@gmail.com>

# Frontend URL (for CORS and email links)
# IMPORTANT: This URL is used for:
#   1. CORS configuration (which origins are allowed)
#   2. Email verification links (where users are redirected)
# 
# Development: 
#   - Single URL: FRONTEND_URL=http://localhost:8080 (matches vite.config.ts port)
#   - For mobile testing: FRONTEND_URL=http://192.168.1.100:8080 (your local IP)
#   - For ngrok: FRONTEND_URL=https://your-subdomain.ngrok.io
# 
# Production: 
#   - Must be publicly accessible HTTPS URL
#   - Example: FRONTEND_URL=https://subsmanager-ai.com
#   - No trailing slash!
# 
# For CORS (multiple origins in production):
#   Production: FRONTEND_URL=https://app.example.com,https://www.example.com
#   (First URL is used for email links, all are used for CORS)
FRONTEND_URL=http://localhost:8080

# Email Verification Token Expiry (in hours)
EMAIL_VERIFY_EXPIRE=24

